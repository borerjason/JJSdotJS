config:
  target: 'http://localhost:8080'
  phases:
    - duration: 3
      arrivalRate: 10
  defaults:
scenarios:
  - flow:
    - get:
        url: "/feed"
        # json: 
          # event : {
          #     user_id: 0,
          #     experimentgroup: 3,
          #     item_id: 0,
          #     itemtype: 'Post',
          #     eventtype: 'Like'
          # }
        
          
